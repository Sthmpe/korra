import 'package:flutter/material.dart';

class KorraColors {
  static const brand = Color(0xFFA54600);
  static const bg = Colors.white;
  static const textSecondary = Color(0xFF8E8E93); // iOS style grey
  static const text = Colors.black87;
  static const textMuted = Color(0xFF636366);

  static const brandDark = Color(0xFF7A3300); // For pressed states
  static const brandLight = Color(0xFFFFF2EB); // For backgrounds
  
  static const white = Colors.white;
  static const black = Color(0xFF1B1B1B);
  static const greyText = Color(0xFF636366);
  static const greyBorder = Color(0xFFE5E5EA);
  static const inputFill = Color(0xFFF9F9F9); // Subtle grey for inputs

  static const success = Color(0xFF1DB954);
  static const error = Color(0xFFE53935);

  static const danger = Color(0xFFE53935);

  static final border = Colors.grey.shade300;
}
